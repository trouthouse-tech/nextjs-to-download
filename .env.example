# No environment variables are required to build or run this app on Vercel or locally.
#
# Optional (unused by default UI; reserved for future localhost tooling):
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:3000
